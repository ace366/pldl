<IfModule mod_rewrite.c>
    RewriteEngine On

    # Avoid rewriting requests that already target /public.
    RewriteCond %{REQUEST_URI} !^/public/

    # Forward all app requests to Laravel's public entry point.
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
